version: "3"

services:
  webserver:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - ./www:/var/www/html
      - "./.htaccess:/var/www/html/.htaccess"